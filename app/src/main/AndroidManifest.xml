<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smart_learn">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presenter.activities.TestLocalActivity"></activity>
        <activity android:name=".presenter.activities.LessonEntriesRVActivity" />
        <activity android:name=".presenter.activities.LessonActivity" />
        <activity android:name=".presenter.activities.LessonRVActivity" />
        <activity android:name=".presenter.activities.OpenLessonActivity" />
        <activity android:name=".presenter.activities.SimpleLoginActivity" />
        <activity android:name=".presenter.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presenter.activities.TestSettingsActivity" />
        <activity android:name=".presenter.activities.TestGenerationActivity" />
        <activity android:name=".presenter.activities.TestOnlineActivity" />
        <activity android:name=".presenter.activities.EntranceRVActivityK" />
        <activity android:name=".presenter.activities.TestActivityK" />
        <activity android:name=".presenter.activities.LessonActivityK" />
        <activity android:name=".presenter.activities.LessonRVActivityK" />
        <activity android:name=".presenter.activities.OpenLessonActivityK" />
    </application>

</manifest>