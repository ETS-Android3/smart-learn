<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smart_learn">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".presenter.helpers.ApplicationController"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presenter.activities.test.user.UserTestActivity"></activity>
        <activity android:name=".presenter.activities.test.guest.GuestTestActivity" />
        <activity android:name=".presenter.activities.test.TestActivity" />
        <activity android:name=".presenter.activities.notebook.user.UserNotebookActivity" />
        <activity android:name=".presenter.activities.notebook.guest.GuestNotebookActivity" />
        <activity android:name=".presenter.activities.notebook.helpers.NotebookActivity" />
        <activity android:name=".presenter.activities.community.CommunityActivity" />
        <activity android:name=".presenter.activities.guest.GuestActivity" />
        <activity android:name=".presenter.activities.authentication.AuthenticationActivity" />
        <activity android:name=".presenter.activities.TestLocalActivity" />
        <activity android:name=".presenter.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presenter.activities.TestSettingsActivity" />
        <activity android:name=".presenter.activities.TestGenerationActivity" />
        <activity android:name=".presenter.activities.TestOnlineActivity" />
    </application>

</manifest>